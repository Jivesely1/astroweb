---
import ThemeToggle from './ThemeToggle.astro';
const currentPath = Astro.url.pathname;
---

<header class="sticky top-0 z-50 backdrop-blur-lg bg-white/80 dark:bg-gray-900/80 border-b border-gray-100 dark:border-gray-800 shadow-sm animate-fade-in-up">
  <nav class="max-w-7xl mx-auto px-4 py-3 sm:py-4">
    <div class="flex items-center justify-between">
      <!-- Logo -->
      <a href="/" class="group flex items-center space-x-2">
        <div class="w-8 h-8 sm:w-10 sm:h-10 bg-gradient-to-br from-blue-600 to-purple-600 rounded-xl flex items-center justify-center text-white font-bold text-base sm:text-xl group-hover:scale-110 group-hover:rotate-12 transition-all duration-300 shadow-lg">
          JV
        </div>
        <span class="hidden sm:inline text-lg sm:text-xl font-bold bg-gradient-to-r from-gray-900 to-gray-600 bg-clip-text text-transparent">
          Jiří Veselý
        </span>
      </a>
      
      <!-- Mobile Menu Button -->
      <button
        id="mobile-menu-btn"
        class="md:hidden p-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors"
        aria-label="Otevřít menu"
        aria-expanded="false"
        aria-controls="mobile-menu"
      >
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
      </button>
      
      <!-- Desktop Navigation -->
      <ul class="hidden md:flex items-center gap-6 lg:gap-8">
        <li>
          <a
            href="/"
            class={`font-medium transition-all duration-300 hover:scale-110 inline-block ${currentPath === '/' ? 'text-purple-600 dark:text-purple-400 font-bold' : 'text-gray-600 dark:text-gray-300 hover:text-purple-600 dark:hover:text-purple-400'}`}
          >
            Domů
          </a>
        </li>
        <li>
          <a
            href="/sluzby"
            class={`font-medium transition-all duration-300 hover:scale-110 inline-block ${currentPath.startsWith('/sluzby') ? 'text-purple-600 dark:text-purple-400 font-bold' : 'text-gray-600 dark:text-gray-300 hover:text-purple-600 dark:hover:text-purple-400'}`}
          >
            Služby
          </a>
        </li>
        <li>
          <a
            href="/projekty"
            class={`font-medium transition-all duration-300 hover:scale-110 inline-block ${currentPath.startsWith('/projekty') ? 'text-purple-600 dark:text-purple-400 font-bold' : 'text-gray-600 dark:text-gray-300 hover:text-purple-600 dark:hover:text-purple-400'}`}
          >
            Portfolio
          </a>
        </li>
        <li>
          <a
            href="/blog"
            class={`font-medium transition-all duration-300 hover:scale-110 inline-block ${currentPath.startsWith('/blog') ? 'text-purple-600 dark:text-purple-400 font-bold' : 'text-gray-600 dark:text-gray-300 hover:text-purple-600 dark:hover:text-purple-400'}`}
          >
            Blog
          </a>
        </li>
        <li>
          <ThemeToggle />
        </li>
        <li>
          <a
            href="/kontakt"
            class="px-5 py-2.5 bg-gradient-to-r from-blue-600 to-purple-600 text-white rounded-xl font-semibold hover:shadow-lg hover:scale-110 transition-all duration-300 inline-block"
          >
            Kontakt 💬
          </a>
        </li>
      </ul>
    </div>
    
    <!-- Mobile Menu -->
    <div id="mobile-menu" class="hidden md:hidden mt-4 pb-4 space-y-3">
      <a
        href="/"
        class={`block px-4 py-2 rounded-lg transition-all duration-300 font-medium ${currentPath === '/' ? 'text-purple-600 dark:text-purple-400 bg-purple-50 dark:bg-gray-800 font-bold' : 'text-gray-600 dark:text-gray-300 hover:bg-purple-50 dark:hover:bg-gray-800 hover:text-purple-600 dark:hover:text-purple-400'}`}
      >
        Domů
      </a>
      <a
        href="/sluzby"
        class={`block px-4 py-2 rounded-lg transition-all duration-300 font-medium ${currentPath.startsWith('/sluzby') ? 'text-purple-600 dark:text-purple-400 bg-purple-50 dark:bg-gray-800 font-bold' : 'text-gray-600 dark:text-gray-300 hover:bg-purple-50 dark:hover:bg-gray-800 hover:text-purple-600 dark:hover:text-purple-400'}`}
      >
        Služby
      </a>
      <a
        href="/projekty"
        class={`block px-4 py-2 rounded-lg transition-all duration-300 font-medium ${currentPath.startsWith('/projekty') ? 'text-purple-600 dark:text-purple-400 bg-purple-50 dark:bg-gray-800 font-bold' : 'text-gray-600 dark:text-gray-300 hover:bg-purple-50 dark:hover:bg-gray-800 hover:text-purple-600 dark:hover:text-purple-400'}`}
      >
        Portfolio
      </a>
      <a
        href="/blog"
        class={`block px-4 py-2 rounded-lg transition-all duration-300 font-medium ${currentPath.startsWith('/blog') ? 'text-purple-600 dark:text-purple-400 bg-purple-50 dark:bg-gray-800 font-bold' : 'text-gray-600 dark:text-gray-300 hover:bg-purple-50 dark:hover:bg-gray-800 hover:text-purple-600 dark:hover:text-purple-400'}`}
      >
        Blog
      </a>
      <button
        id="mobile-theme-toggle"
        class="block w-full px-4 py-2 text-gray-600 dark:text-gray-300 hover:bg-purple-50 dark:hover:bg-gray-800 hover:text-purple-600 dark:hover:text-purple-400 rounded-lg transition-all duration-300 font-medium text-left"
        aria-label="Přepnout světlý/tmavý režim"
      >
        <span class="flex items-center gap-2">
          <svg id="mobile-theme-icon-light" class="w-5 h-5 hidden" fill="currentColor" viewBox="0 0 20 20">
            <path d="M10 2a1 1 0 011 1v1a1 1 0 11-2 0V3a1 1 0 011-1zm4 8a4 4 0 11-8 0 4 4 0 018 0zm-.464 4.95l.707.707a1 1 0 001.414-1.414l-.707-.707a1 1 0 00-1.414 1.414zm2.12-10.607a1 1 0 010 1.414l-.706.707a1 1 0 11-1.414-1.414l.707-.707a1 1 0 011.414 0zM17 11a1 1 0 100-2h-1a1 1 0 100 2h1zm-7 4a1 1 0 011 1v1a1 1 0 11-2 0v-1a1 1 0 011-1zM5.05 6.464A1 1 0 106.465 5.05l-.708-.707a1 1 0 00-1.414 1.414l.707.707zm1.414 8.486l-.707.707a1 1 0 01-1.414-1.414l.707-.707a1 1 0 011.414 1.414zM4 11a1 1 0 100-2H3a1 1 0 000 2h1z"></path>
          </svg>
          <svg id="mobile-theme-icon-dark" class="w-5 h-5 hidden" fill="currentColor" viewBox="0 0 20 20">
            <path d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z"></path>
          </svg>
          <span id="mobile-theme-text">Světlý režim</span>
        </span>
      </button>
      <a href="/kontakt" class="block px-4 py-2 bg-gradient-to-r from-blue-600 to-purple-600 text-white rounded-lg font-semibold text-center">
        Kontakt 💬
      </a>
    </div>
  </nav>
</header>

<script>
  function initMobileMenu() {
    const btn = document.getElementById('mobile-menu-btn');
    const menu = document.getElementById('mobile-menu');

    if (btn && menu) {
      btn.addEventListener('click', () => {
        const isExpanded = btn.getAttribute('aria-expanded') === 'true';

        menu.classList.toggle('hidden');
        btn.setAttribute('aria-expanded', (!isExpanded).toString());
        btn.setAttribute('aria-label', !isExpanded ? 'Zavřít menu' : 'Otevřít menu');
      });
    }
  }

  function initMobileThemeToggle() {
    const mobileThemeBtn = document.getElementById('mobile-theme-toggle');
    const mobileLightIcon = document.getElementById('mobile-theme-icon-light');
    const mobileDarkIcon = document.getElementById('mobile-theme-icon-dark');
    const mobileThemeText = document.getElementById('mobile-theme-text');

    if (!mobileThemeBtn || !mobileLightIcon || !mobileDarkIcon || !mobileThemeText) return;

    // Update mobile theme UI
    function updateMobileThemeUI() {
      const isDark = document.documentElement.classList.contains('dark');

      if (isDark) {
        mobileLightIcon.classList.remove('hidden');
        mobileDarkIcon.classList.add('hidden');
        mobileThemeText.textContent = 'Světlý režim';
      } else {
        mobileLightIcon.classList.add('hidden');
        mobileDarkIcon.classList.remove('hidden');
        mobileThemeText.textContent = 'Tmavý režim';
      }
    }

    // Initialize UI
    updateMobileThemeUI();

    // Handle click
    mobileThemeBtn.addEventListener('click', () => {
      if (window.handleThemeToggle) {
        window.handleThemeToggle();
        updateMobileThemeUI();
      }
    });
  }

  // Initialize on page load
  initMobileMenu();
  initMobileThemeToggle();

  // Re-initialize after view transitions
  document.addEventListener('astro:page-load', () => {
    initMobileMenu();
    initMobileThemeToggle();
  });
</script>